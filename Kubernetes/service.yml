apiVersion: v1
kind: Service
metadata:
  name: DevSecOps-Project
  labels:
    app: DevSecOps-Project
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: DevSecOps-Project
